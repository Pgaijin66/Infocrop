<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="crop.parser.com.crop_final" >

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainScreenActivity" />

        <!-- All Product Activity -->
        <activity
            android:name=".AllProductsActivity"
            android:label="All Products" >
        </activity>

        <!-- Add Product Activity -->
        <activity
            android:name=".NewProductActivity"
            android:label="Add New Product" >
        </activity>

        <!-- Edit Product Activity -->

        <activity
            android:name=".EditProductActivity"
            android:label="Edit Product" >
        </activity>
        <activity
            android:name=".GraphActivity"
            android:label="@string/title_activity_graph" >
        </activity>
        <activity
            android:name=".cultivatedVSaffected"
            android:label="@string/title_activity_cultivated_vsaffected" >
        </activity>
        <activity
            android:name=".yearsVSproduction"
            android:label="@string/title_activity_years_vsproduction" >
        </activity>
        <activity
            android:name=".ListViewDetail"
            android:label="@string/title_activity_list_view_detail" >
        </activity>
        <activity
            android:name=".ForumActivity"
            android:label="@string/title_activity_forum" >
        </activity>
        <activity
            android:name=".FeasibilityActivity"
            android:label="@string/title_activity_feasibility" >
        </activity>
        <activity
            android:name=".NewsActivity"
            android:label="@string/title_activity_news" >
        </activity>
    </application>

</manifest>
